interface:
  display_name: "Spec Design"
  short_description: "For capability specs before code design."
  default_prompt: "Use $spec-design when a PRD, feature, bug, or existing spec needs a stable behavior contract before code design."
